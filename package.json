{
  "name": "time-to-review",
  "version": "1.0.0",
  "description": "GitHub Action to estimate pull request review time and add time-based labels",
  "main": "index.js",
  "scripts": {
    "build": "ncc build time-to-review.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github",
    "action",
    "pull-request",
    "review",
    "estimate",
    "label"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.36.1"
  }
}
